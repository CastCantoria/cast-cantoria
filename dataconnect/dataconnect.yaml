specVersion: "v1"
serviceId: "cast-cantoria"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cast-cantoria-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./example"]
